var radiusNode = 50; // Radius of Each Nodes
var frameHeight = 50;  // Height of the upper frame

var Array_Random = []; // Random Arrays Generated By Engine.
var Sorted_Array = []; // Sorted Array stored

var tree; // TREE OBJECT
var height; // NUMBER OF LEVELS IN TREE

var Grid; // GRID SIZE
var gridArray = [];
var MaxValues = 0;

function setup(){

    var canvas = createCanvas(window.innerWidth, window.innerHeight); // smaller then the full window.
    var x = (windowWidth - width) / 2; // finding the center coordinates
    var y = (windowHeight - height) / 2; 
    //console.log(y);

    MaxValues = round(innerWidth / 50) - 2;
    console.log(MaxValues + " MaxValues");
    tree = new Tree();
    randomArray();
    
    // height = tree.height();
    // Grid = Math.pow(2, height);
    // console.log(Grid, height, "imp");
    
    // for(var i=0; i<Grid+1;i++){
    //     gridArray.push(i*((innerWidth-50)/(Grid+1))+25);
    // }
    // console.log(gridArray);
}

function draw(){

    background(25);

    push();
    fill(255);
    frameUpper();
    pop();

    drawTree();
}

function randomArray(){
    Array_Random = [];

    for(var i = 0; i < MaxValues; i++){
        var temp = floor(random(10, 99))
        Array_Random.push(temp);
        tree.addValues(temp);
    }

    // console.log(tree);
    //tree.traverse();
    //Sorted_Array = Array_Random.sort();
}

function frameUpper(){
    rect(0, 0, innerWidth, frameHeight);
    for(var i = 0; i < MaxValues ; i++){
        push();
        fill(0);
        text(Array_Random[i], i*50+50, 25);
        pop();
    }
}

function drawTree(){
    tree.draw();
}

function drawNode(x, y){
    fill(255);  
    circle(x, y, radiusNode);
}